#pragma once
#include "pch.h"
//#include "StructManager.h"
//
//Vector3 NormalizedPosToLocalPos(Vector3 normalizedPos, Vector2 rectSizeDelta, Vector2 rectPivot, bool applyPivot = true);
//
//Vector3 LocalPosToNormalizedPos(Vector3 localPos, Vector2 rectSizeDelta, Vector2 rectPivot, bool applyPivot = true);
//
//Vector3 GetProjectedPosition(Vector3 position, Vector3 planeRotation);
//
//Vector3 ApplyCameraRotation(Vector3 position, Vector3 cameraRotation, bool useZProjection);

//bool GetOverlappedPoint(Vector3 line1StartPos, Vector3 line1EndPos, Vector3 line2StartPos, Vector3 line2EndPos, Vector3 *overlappedPos);

//bool CheckRangeOut(Vector2 rectSizeDelta, Vector2 rectLocalPoisition, Vector3 position);

//void SolveLinearEquations(float a, float b, float c, float d, float e, float f, float *x, float *y);

//Vector3 ClampVector(Vector3 point, Vector3 rectSizeDelta);

//LineInfo SetLineColor(LineInfo lineInfo, Color startPointColor, Color endPointColor);

//void CreateLineVertex(Vector3* vertices, Color* colors, int* vertexCount, LineInfo lineInfo);

//LineInfo AdjustLineEdge(LineInfo lineInfo, ConfigHandle configHandle, bool isStartPoint);

//void AddLineConrerVertex(Vector3* vertices, Color* colors, int* vertexCount, LineInfo previousLineInfo, LineInfo lineInfo, ConfigHandle configHandle);